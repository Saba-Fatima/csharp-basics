#region count a total number of duplicate elements in an array
			//int[] arr1 = new int[100];
			//int count = 0,n;
			//Console.WriteLine("Enter size of the array:");
			//n = Convert.ToInt32(Console.ReadLine());
			//Console.WriteLine("Add elements to  the array:");
			//for (int i = 0; i < n; i++)
			//{
			//	arr1[i] = Convert.ToInt32(Console.ReadLine());
			//}
			//Console.WriteLine("check for duplicate elements in an array:");
			//for (int i = 0; i < n; i++)
			//{
			//	for (int j = 0; j < i; j++)
			//	{
			//		if (arr1[i]==arr1[j])
			//		{
			//			count++;
			//		}
			//	}
			//}
			//Console.WriteLine(count);
			//Console.Read();
			#endregion
